#include <bits/stdc++.h>
using namespace std;

#define ll long long

int main() {
    ifstream is("aoc/2024/d1.txt");
    string str;
    while (getline(is, str)) {
        
    }

    int sum;
    return 0;
}